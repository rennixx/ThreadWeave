# Video Settings
output_resolution: "1080x1920"  # vertical for TikTok/Reels
video_fps: 30
video_bitrate: "8M"
audio_bitrate: "192k"

# Art Style (optimized for social media thread videos)
art_style: "watercolor painting, soft flowing organic shapes, gentle gradients, layered translucent washes, dreamy atmosphere, pastel color palette, elegant brush strokes"

# Scene Generation
default_duration: 30  # seconds
min_scenes: 6
max_scenes: 10
scene_min_duration: 2.0
scene_max_duration: 5.0

# Image Generation
# Available models: Flux.1-schnell (fastest), SDXL Lightning, Stable Diffusion 3
image_model: "stabilityai/sdxl-turbo"  # Standard SDXL Turbo, publicly available
image_model_type: "auto"  # auto-detect or: flux, sdxl, sdt3
image_steps: 4  # Steps for generation (Flux: 4, SD3: 28, SDXL: 4)
image_cfg_scale: 0.0  # Guidance scale (Flux: 0.0, SD3: 7.0, SDXL: 0.0)
image_resolution: [768, 1024]  # width, height
use_consistent_seed: true
base_seed: 42
use_torch_compile: false  # Disable torch.compile (requires Triton)
max_sequence_length: 256  # For Flux prompt processing

# Animation
enable_camera_movements: true
enable_frame_interpolation: true  # Enable frame interpolation for smoother animation
interpolation_frames: 12  # Frames to interpolate between keyframes
transition_duration: 0.5  # seconds

# AI Animation (advanced - requires more VRAM and time)
use_ai_animation: false  # Use AI models for true frame-by-frame animation
ai_animation_model: "svd"  # Options: "svd" (Stable Video Diffusion), "animatediff"

# Audio
audio_volume_narration: 0  # dB
audio_volume_music: -22  # dB
tts_provider: "edge"  # or "openai", "elevenlabs", "local"
tts_voice: "en-US-AriaNeural"  # for Edge TTS (free)

# Paths
background_music_path: "assets/background_music/default.mp3"

# Performance
enable_gpu: true
gpu_memory_fraction: 0.9
enable_xformers: true

# Local LLM (Ollama)
ollama_base_url: http://localhost:11434
ollama_model: ministral-3
